# Website

React + Vite + Tailwind scaffold for XCEED solutions website.

## Local dev
1. `npm install`
2. `npm run dev`

## Build & deploy
- Set `VITE_BASE` environment var to `/your-repo-name/` if deploying to GitHub Pages.
- `npm run build`
- `npm run deploy` (uses gh-pages)
